import 'package:flutter/material.dart';
import 'package:sdgp_test01/core/app_export.dart';

class Frame639Screen extends StatelessWidget {
  const Frame639Screen({Key? key})
      : super(
          key: key,
        );

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: SizedBox(
          height: 788.v,
          width: double.maxFinite,
        ),
      ),
    );
  }
}
